
public class Main {

    /**
     * The main entry point of the program
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        //Create a new instance of the StudentManager class
        StudentManager manager = new StudentManager();
        //Call the addStudentInformation method to start adding student information
        manager.addStudentInformation();
    }
    
}
