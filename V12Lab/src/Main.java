public class Main {

    /**
     * The main method serves as the entry point for the program.
     * It creates an instance of the Menu class and calls its show() method.
     * The show() method displays the menu and interacts with the user.
     * 
     * @param args args the command line arguments
     * @throws Exception
     */
    public static void main(String[] args) throws Exception {
        // Create an instance of the Menu class
        Menu menu = new Menu();
        // Call the show() method to display the menu and interact with the user
        menu.show();
    }
}
